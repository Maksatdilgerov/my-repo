<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Docker Practice with DevOps Batch1</title>
  <link rel="stylesheet" href="/theme.css">
</head>
<body>
  <nav class="navbar">
    <h3>Congratulations you've done a great job</h3>
  </nav>

  <div class="container with-nav">
    <div class="card">
      <h2>Techtorial docker practice by Ethan</h2>

      <form action="/" method="post">
        <div class="form-control">
          <input name="text" type="text" placeholder="add Techtorial Student name here... to be honest you can add it a multiple time">
        </div>

        <button type="submit" class="btn primary">Add</button>
      </form>

      <hr />

      <% if(logs.length === 0){ %>
        <p>No logs now</p>
      <% } %>

      <ul>
        <% for (let i = 0; i < logs.length; i++) {%>
          <li><%= logs[i] %></li>
        <% } %>
      </ul>
    </div>
  </div>
</body>
</html>